version: "1.0"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: mongodb+srv://meetmshah88:gJiS1jksOe46VG36@cluster0.mh0nb.mongodb.net/userManagement
      NODE_ENV: "production"
    depends_on:
      - frontend

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      # 80 is the default port Nginx serves, mapped to 3000
      - "3000:80"
    environment:
      VITE_BACKEND_URL: "http://localhost:5000"
